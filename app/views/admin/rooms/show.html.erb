<%= content_tag(:div, class: "row") do %>
  <%= content_tag(:div, class: "col-xl-6 col-md-12") do %>
    <%= content_tag(:div, class: "card") do %>
      <%= render(Card::HeaderComponent.new(title: @room.name, color: "primary")) %>
      <%= content_tag(:div, class: "card-body") do %>
        <%= @room.description %>
      <% end %>
    <% end %>
    <%= link_to 'Editar', edit_room_path(@room), class: "btn btn-primary pull-right" %>
    <%= link_to 'Volver', rooms_path, class: "btn" %>
  <% end %>
<% end %>