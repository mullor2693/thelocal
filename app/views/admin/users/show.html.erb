<%= tag.div(class: "row") do %>
  <%= content_tag(:div, class: "col-xl-6 col-md-12") do %>
    <%= content_tag(:div, class: "card") do %>
      <%= render(Headers::AvatarComponent.new(user: @user)) %>
      <%= content_tag(:div, class: "card-body") do %>        
        <%= content_tag(:p, "Email: #{@user.email}") %>
        <%= content_tag(:p, "Phone: #{@user.phone}") %>
        <%= content_tag(:p, "Birhdate: #{@user.birth_date}") %>
        <%#= content_tag(:div, class: "text-right") do %>
          <%#= render(Button::SmallComponent.new(link: (current_user == @user ? edit_user_registration_path : edit_admin_user_path(@user)), title: "Editar", icon: "edit", color: "white" )) %>
        <%# end %>
      <% end %>
    <% end %>
    <%= link_to 'Editar', (current_user == @user ? edit_user_registration_path : edit_admin_user_path(@user)), class: "btn btn-primary pull-right" %>
    <%= link_to 'Volver', admin_users_path, class: "btn" %>
  <% end %>
<% end %>

<div class="uk-algin-center" uk-grid>
  <div class="uk-grid-match uk-width-1-2@s uk-width-2-3@l uk-width-3-4@xl" uk-grid>
    <% [ 
      # ['Dietas', admin_user_diets_path(@user)],
      ['Evaluaciones', admin_user_evaluations_path(@user)],
      ['Medidas', admin_user_measurements_path(@user)],
      ['Pesos', admin_user_weights_path(@user)],
      ['Workouts', admin_user_workouts_path(@user)]
    ].each do |link_url| %>
      <div class="uk-grid-match uk-width-1-2@s uk-width-1-3@l uk-width-1-4@xl">
        <%= link_to link_url[1], class:"uk-card uk-card-default uk-card-small uk-card-hover uk-card-body uk-link-reset uk-text-center" do %>
            <h3 class="uk-card-title"><%= link_url[0] %></h3>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
